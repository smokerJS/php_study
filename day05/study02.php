<?
  // PHP의 내장함수

  // 수학관련 내장함수
  abs() // 절댓값 계산
  sin() // 삼각형의 사인값 계산
  cos() // 삼각형의 코사인값 계산
  tan() // 삼각형의 탄젠트값 계산
  date() // 현재 날짜 계산
  ceil() // 소수점 아래에서 올린 정수값 계산
  floor() // 소수점 아래에서 내린 정수값 계산
  round() // 반올림값 계산
  log() // 로그값 계산
  max() // 최댓값 계산
  sqrt() // 제곱근(square root) 계산
  rand() // 임의의 난수 생성
  pi() // 파이의 근사값(3.141592) 계산
?>